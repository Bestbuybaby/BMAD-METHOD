interface:
  display_name: "BMAD METHOD"
  short_description: "Repo-specific patterns and workflows for BMAD-METHOD"
  default_prompt: "Use the BMAD-METHOD repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true